server:
  port: ${PORT}
  assets-path: ./assets

theme:
  custom-css-file: /assets/style.css

pages:
  !include: pages/home.yml
  !include: pages/f1.yml

  # Add more pages here:
  # - name: Your page name
  #   columns:
  #     - size: small
  #       widgets:
  #         # Add widgets here

  #     - size: full
  #       widgets:
  #         # Add widgets here

  #     - size: small
  #       widgets:
  #         # Add widgets here
